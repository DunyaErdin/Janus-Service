services:
  edge-ai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: janus-edge-ai
    init: true
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8080:8080"
    stop_grace_period: 15s
